/*
Deployment script for eGIFT

This code was generated by a tool.
Changes to this file may cause incorrect behavior and will be lost if
the code is regenerated.
*/

GO
SET ANSI_NULLS, ANSI_PADDING, ANSI_WARNINGS, ARITHABORT, CONCAT_NULL_YIELDS_NULL, QUOTED_IDENTIFIER ON;

SET NUMERIC_ROUNDABORT OFF;


GO
:setvar DatabaseName "eGIFT"
:setvar DefaultFilePrefix "eGIFT"
:setvar DefaultDataPath "C:\Program Files\Microsoft SQL Server\MSSQL12.MSSQLSERVER\MSSQL\DATA\"
:setvar DefaultLogPath "C:\Program Files\Microsoft SQL Server\MSSQL12.MSSQLSERVER\MSSQL\DATA\"

GO
:on error exit
GO
/*
Detect SQLCMD mode and disable script execution if SQLCMD mode is not supported.
To re-enable the script after enabling SQLCMD mode, execute the following:
SET NOEXEC OFF; 
*/
:setvar __IsSqlCmdEnabled "True"
GO
IF N'$(__IsSqlCmdEnabled)' NOT LIKE N'True'
    BEGIN
        PRINT N'SQLCMD mode must be enabled to successfully execute this script.';
        SET NOEXEC ON;
    END


GO
USE [$(DatabaseName)];


GO
/*
The column [dbo].[Products].[A Project which specifies] is being dropped, data loss could occur.

The column [dbo].[Products].[merchantId] on table [dbo].[Products] must be added, but the column has no default value and does not allow NULL values. If the table contains data, the ALTER script will not work. To avoid this issue you must either: add a default value to the column, mark it as allowing NULL values, or enable the generation of smart-defaults as a deployment option.

The column [dbo].[Products].[name] on table [dbo].[Products] must be added, but the column has no default value and does not allow NULL values. If the table contains data, the ALTER script will not work. To avoid this issue you must either: add a default value to the column, mark it as allowing NULL values, or enable the generation of smart-defaults as a deployment option.

The column [dbo].[Products].[price] on table [dbo].[Products] must be added, but the column has no default value and does not allow NULL values. If the table contains data, the ALTER script will not work. To avoid this issue you must either: add a default value to the column, mark it as allowing NULL values, or enable the generation of smart-defaults as a deployment option.

The type for column Id in table [dbo].[Products] is currently  INT NOT NULL but is being changed to  UNIQUEIDENTIFIER NOT NULL. There is no implicit or explicit conversion.
*/

IF EXISTS (select top 1 1 from [dbo].[Products])
    RAISERROR (N'Rows were detected. The schema update is terminating because data loss might occur.', 16, 127) WITH NOWAIT

GO
PRINT N'Rename refactoring operation with key d279d009-c189-4ba8-977f-55390d59b6d1 is skipped, element [dbo].[Orders].[RecipientAddress] (SqlSimpleColumn) will not be renamed to RecipientEmailAddress';


GO
PRINT N'Starting rebuilding table [dbo].[Products]...';


GO
BEGIN TRANSACTION;

SET TRANSACTION ISOLATION LEVEL SERIALIZABLE;

SET XACT_ABORT ON;

CREATE TABLE [dbo].[tmp_ms_xx_Products] (
    [Id]          UNIQUEIDENTIFIER DEFAULT NEWID() NOT NULL,
    [merchantId]  UNIQUEIDENTIFIER NOT NULL,
    [name]        NVARCHAR (MAX)   NOT NULL,
    [description] NVARCHAR (MAX)   NULL,
    [price]       DECIMAL (13, 2)  NOT NULL,
    PRIMARY KEY CLUSTERED ([Id] ASC)
);

IF EXISTS (SELECT TOP 1 1 
           FROM   [dbo].[Products])
    BEGIN
        INSERT INTO [dbo].[tmp_ms_xx_Products] ([Id])
        SELECT   [Id]
        FROM     [dbo].[Products]
        ORDER BY [Id] ASC;
    END

DROP TABLE [dbo].[Products];

EXECUTE sp_rename N'[dbo].[tmp_ms_xx_Products]', N'Products';

COMMIT TRANSACTION;

SET TRANSACTION ISOLATION LEVEL READ COMMITTED;


GO
PRINT N'Creating [dbo].[Merchants]...';


GO
CREATE TABLE [dbo].[Merchants] (
    [Id]      UNIQUEIDENTIFIER NOT NULL,
    [Name]    NVARCHAR (256)   NOT NULL,
    [Address] NVARCHAR (MAX)   NULL,
    PRIMARY KEY CLUSTERED ([Id] ASC)
);


GO
PRINT N'Creating [dbo].[Orders]...';


GO
CREATE TABLE [dbo].[Orders] (
    [Id]                    UNIQUEIDENTIFIER NOT NULL,
    [ProductId]             UNIQUEIDENTIFIER NOT NULL,
    [RecipientName]         NVARCHAR (MAX)   NOT NULL,
    [RecipientEmailAddress] NVARCHAR (MAX)   NOT NULL,
    [SenderName]            NVARCHAR (MAX)   NOT NULL,
    [SenderEmailAddress]    NVARCHAR (MAX)   NOT NULL,
    [DateOrdered]           DATETIME         NOT NULL,
    PRIMARY KEY CLUSTERED ([Id] ASC)
);


GO
PRINT N'Creating unnamed constraint on [dbo].[Merchants]...';


GO
ALTER TABLE [dbo].[Merchants]
    ADD DEFAULT NEWID() FOR [Id];


GO
PRINT N'Creating unnamed constraint on [dbo].[Merchants]...';


GO
ALTER TABLE [dbo].[Merchants]
    ADD DEFAULT '' FOR [Name];


GO
PRINT N'Creating unnamed constraint on [dbo].[Orders]...';


GO
ALTER TABLE [dbo].[Orders]
    ADD DEFAULT NEWID() FOR [Id];


GO
PRINT N'Creating [dbo].[Fk_Products_ToMerchant]...';


GO
ALTER TABLE [dbo].[Products] WITH NOCHECK
    ADD CONSTRAINT [Fk_Products_ToMerchant] FOREIGN KEY ([merchantId]) REFERENCES [dbo].[Merchants] ([Id]);


GO
PRINT N'Creating [dbo].[FK_Orders_Products]...';


GO
ALTER TABLE [dbo].[Orders] WITH NOCHECK
    ADD CONSTRAINT [FK_Orders_Products] FOREIGN KEY ([ProductId]) REFERENCES [dbo].[Products] ([Id]);


GO
-- Refactoring step to update target server with deployed transaction logs

IF OBJECT_ID(N'dbo.__RefactorLog') IS NULL
BEGIN
    CREATE TABLE [dbo].[__RefactorLog] (OperationKey UNIQUEIDENTIFIER NOT NULL PRIMARY KEY)
    EXEC sp_addextendedproperty N'microsoft_database_tools_support', N'refactoring log', N'schema', N'dbo', N'table', N'__RefactorLog'
END
GO
IF NOT EXISTS (SELECT OperationKey FROM [dbo].[__RefactorLog] WHERE OperationKey = 'd279d009-c189-4ba8-977f-55390d59b6d1')
INSERT INTO [dbo].[__RefactorLog] (OperationKey) values ('d279d009-c189-4ba8-977f-55390d59b6d1')

GO

GO
PRINT N'Checking existing data against newly created constraints';


GO
USE [$(DatabaseName)];


GO
ALTER TABLE [dbo].[Products] WITH CHECK CHECK CONSTRAINT [Fk_Products_ToMerchant];

ALTER TABLE [dbo].[Orders] WITH CHECK CHECK CONSTRAINT [FK_Orders_Products];


GO
PRINT N'Update complete.';


GO
